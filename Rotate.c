#include<stdio.h>
int main(){
    int r,c;
    printf("Enter the row and coloumn of the matrix: ");
    scanf("%d %d",&r,&c);
    int arr[r][c];
    printf("Enter the elements of the array\n");
    for(int i=0;i<r;i++)
    {
        for(int j=0;j<c;j++)
        scanf("%d",)
    }
}